# 旧双 DLL 工具存档

这里保存原来用于 `CampusUno.dll` + `CampusMinigames.UP.dll` 的验证、审阅安装和记录脚本，改用 `.py.txt`，避免误当作当前工具执行。原文件保留用于查历史行为。

它们依赖私人 QA 副本、旧构建目录或旧宿主 API，不能用于现在的 Steam 包。当前入口见 [仓库与发布结构](../../REPOSITORY_LAYOUT.md)。
