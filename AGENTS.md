# 小游戏插件维护约定

## 检索与记忆

- 已知符号先定向查源码；需要背景时运行 `python3 -B tools/project_memory.py search '具体问题'`。默认只取 3 段，每段最多 550 字，最多改写一次查询；命中后核对源码。
- 首次使用先 `index`；改动现状、接口、约定或验收结果后，替换相应 `docs/knowledge/` 条目并增量 `index`。不要把聊天、逐次操作流水、旧版本状态堆进现行知识。
- `status --check` 检查过期，`compact` 回收空闲页；只索引明确白名单。`docs/design/` 保留详细历史设计要求，按需阅读；历史资料与 QA 不自动进索引。见 `docs/PROJECT_MEMORY.md`。
- 仓库共享知识原文、白名单和重建工具；`.project-memory/` 是可重建的本机缓存，不上传私人旧数据库或查询记录。

## 接近完工时的多 agent 审计（必须执行）

- 每款小游戏主要功能完成、可构建且基本流程可走通时，主 agent 主动创建 3 个子 agent：UI 与操作、玩法与 CFG/UP 接入、稳定性与发布兼容。具体职责和报告格式见 `docs/workflow/完工审计.md`。
- 子 agent 并行审查明确范围，不相互复制任务，不再递归派生审计 agent。主 agent 同时做构建或受影响的回归。默认一轮集中审计，修复后最多一轮针对原问题的复核；普通小修不重跑整组。
- 阻断崩溃、丢档、错误结算/奖励、注册或配置失效、主流程无法操作等有证据的严重问题。轻微视觉偏好、无依据猜测和范围外重构不阻断交付；可复现且明显影响操作的 UI 问题必须修复。
- 不以“零建议”作为结束条件，不因措辞、主观评分或同一条已验证建议反复重试。保留每轮明确结论、证据和未验证项；审计不能替代真实运行，不得把缺少实机证据写成通过。
- 仍有阻断项时继续针对该问题修复并说明状态，不通过增加整轮审计来消耗时间。发布仍遵循用户本次授权。

## 结构与兼容

- 当前布局以 Steam 工坊 3802190178 的 0.12.0 发布包为基准，详见 `docs/REPOSITORY_LAYOUT.md`。Fable 5.1 完成了单 DLL、JSON 配置和剧情启动整合；后续整理不能退回旧双 DLL 结构。
- UNO 与其他小游戏同属 `src/Games/`，共同编译为 `CampusMinigames.dll`。不要给 UNO 恢复单独插件、安装目录、发布包或独立启动入口。
- 工坊包固定使用 `plugins/`、`EC2BUnofficialPatch/Minigame/`、`Cfgs/zh-cn/`、`readme/`、`preview.jpg`、`manifest.json`。UP 是外部前置，不附带 UP DLL。
- 游戏参数放在 `mod/Cfgs/zh-cn/`；修改默认值时检查 `tools/tuning/settings.json` 与生成结果。作者自建 Mod 的表覆盖默认配置，不能写回覆盖作者文件。
- 稳定的 GUID、命名空间、注册类型名、游戏编号、存档标识、嵌入资源名不是文件夹名称；不能为了目录美观直接修改它们。
- 源码、资源、配置、测试、工具、文档分别放入 `src/`、`assets/`、`mod/`、`tests/`、`tools/`、`docs/`。构建产物留在忽略的 `dist/`，历史材料放 `docs/archive/`。
- `docs/archive/legacy-tools/` 是旧双 DLL 验证和部署脚本的文本存档，不是当前发布入口。不得对真实游戏运行旧部署流程。
- 目录变更必须同步更新编译、项目引用、生成器、打包和 CI；运行规则测试及 `python3 tools/check_layout.py`，有游戏引用时编译并检查组装包。源码/规则检查与原生游戏验收要分别报告。
- 保留贡献归属、素材来源和许可证；不要上传游戏程序集、玩家模组、存档、日志、账号数据或 agent 临时文件。
